import { Dimensions } from 'react-native';

const WIDTH = Dimensions.get('window').width / 100 * 20;
const HEIGHT = Dimensions.get('window').height / 100 * 10;
const FONT = Dimensions.get('window').width / 100 * 7;



export { WIDTH, HEIGHT, FONT }